import { style } from "@vanilla-extract/css";

export const detailsContainer = style({
  padding: "20px",
});

export const coverImg = style({
  width: "100%",
  height: "auto",
  borderRadius: "10px",
});
